repository:
  root: C:/Users/Dean/source/Aspire-Full - 1
  branch: master
dotnet:
  sdk: 10.0.100
  preview: true
python:
  version: 3.14.0
  manager: uv
  torch:
    device: cpu
docker:
  registry: localhost:5000
  image_prefix: aspire-agents
  images:
    python:
      free_threading:
        bleeding_edge: python:3.14.0a1-slim
        stable: python:3.13.0-slim
        note: Requires --disable-gil build flag
hardware:
  gpu:
    enabled: true
    driver: 581.57
