repository:
  root: "."  # Relative to workspace root - fully portable
  branch: "master"
dotnet:
  sdk: "10.0.100"
  preview: true
python:
  version: "3.15t"  # Free-threading build - GIL disabled by default
  manager: "uv"
  gil_disabled: true
  jit_enabled: true
  torch:
    device: "cuda"  # Updated to match hardware.gpu.enabled
    cuda_arch_list: "7.0 7.5 8.0 8.6 8.9 9.0+PTX"
docker:
  registry: "localhost:5000"
  image_prefix: "aspire-agents"
hardware:
  gpu:
    enabled: true
    driver: "581.57"
    compute_capability: "8.6"
