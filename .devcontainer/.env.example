# GitHub Actions Self-Hosted Runner Configuration
# Copy this file to .env and fill in your values

# Required: GitHub Personal Access Token
# Create at: https://github.com/settings/tokens/new
# Required scopes: repo, workflow
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Required: Repository to register runner with (owner/repo format)
GITHUB_REPOSITORY=DeanLuus22021994/Aspire-Full

# Optional: Runner name (defaults to hostname-based name)
RUNNER_NAME=aspire-runner

# Optional: Runner labels (comma-separated)
RUNNER_LABELS=self-hosted,Linux,X64,docker,dotnet,aspire

# Optional: Runner group (for organization runners)
RUNNER_GROUP=Default
